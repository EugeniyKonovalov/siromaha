const Footer = () => {
  return (
    <div className="h-20 w-full mt-auto flex items-center justify-center">
      &copy;{new Date().getFullYear()} Siromaha
    </div>
  );
};

export default Footer;
